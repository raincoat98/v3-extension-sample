rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    // 개발/테스트용: 모든 읽기/쓰기 허용
    // ⚠️ 프로덕션 환경에서는 더 엄격한 규칙을 설정하세요
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
